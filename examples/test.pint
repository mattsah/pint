{
	Declare the unit
}
unit main;

{
	Import examples
}

{ Single line }
uses io;

{ Multi line }
uses
	{ Entire unit }
	math,
	{ Unit as alias }
	regex as rx,
	{ Individual identifier }
	app\models.Person,
	{ Individual identifier as alias }
	app\models.PersonRepository as People,
	{ Multiple identifiers optionally alias }
	app\models(
		Car,
		CarRepository as Cars
	)
;

register main: function(argc: integer; argv: string[]): void
begin
  test;
end

