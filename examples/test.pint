unit main;

uses
	io,
	crypt\utils.rot13 as encrypt,
	crono.time,
	math(
		average as avg,
		floor,
		ceil
	)
;

{
	Testing	
}
register main: function(argc: integer; argv: string[]): integer
begin
	io.writeLn('Hello %s!'.format('World').lower()[1].upper()[1]);
end
