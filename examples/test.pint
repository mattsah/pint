{
	Declare the unit
}
unit app;

{
	Import examples
}

{ Single line }
uses io;

{ Multi line }
uses
	{ Entire unit }
	math,
	{ Unit as alias }
	regex as rx,
	{ Individual identifier }
	app\models.Person,
	{ Individual identifier as alias }
	app\models.PersonRepository as People
;