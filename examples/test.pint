{
	Declare the unit
}
unit app;

{
	Import examples
}
uses io; // Single line
uses     // Multi line
	     // Entire unit
	math,
	     // Unit as alias
	regex as rx,
	     // Individual identifier
	app\models.Person,
	     // Individual identifier as alias
	app\models.PersonRepository as People
;