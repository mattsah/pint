unit main;

uses io;

{
	The main function is the entry point.
}
const main: function(): int
	begin
		var x: int; // Declare x as an int
		var y: int;
		var z: float;

		x = 2;
		y = 2;
		z = x / y;

		if y + 2 == 5
			io.writeLn("failed basic math");
		else
			io.writeLn('%d + %d // = %d'.format(x, y, x + y));

		return 0;
	end;